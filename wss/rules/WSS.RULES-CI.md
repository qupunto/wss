# CI judge — held rules

Rules held by `verify.yml`: a check exists today and can detect a breach.

A row moves here from `prospective/WSS.RULES-CI.md` when its check is built.

**Source:** wss/logs/WSS.DECISIONS.md's `2026-08-17 (eighth)` entry.
