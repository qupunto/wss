# DOC judge — held rules

Rules held by `wss/tests/wss-doctor.sh`: a check exists today and can detect a breach.

A row moves here from `prospective/WSS.RULES-DOC.md` when its check is built.

**Source:** wss/logs/WSS.DECISIONS.md's `2026-08-17 (eighth)` entry.
