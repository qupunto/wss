# DOC judge — definable rules

Rules for `wss/tests/wss-doctor.sh` that apply and bind, but nothing detects a breach yet. A check could exist.

A row moves to top-level `WSS.RULES-DOC.md` when its check is built.

### DOC-DOC-001

**statement:** A new mechanical check lands warn-grade, with the condition for promoting it to a failure written into its own section.

**kind:** requirement

**tier:** scoped

**custody:** DOC · definable

**consequence:** report

**evidence:** wss/logs/WSS.DECISIONS.md's `2026-08-18 (twenty-third)` entry

**Source:** wss/logs/WSS.DECISIONS.md's `2026-08-17 (eighth)` entry.
