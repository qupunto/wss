# APPEND judge — definable rules

Rules for `wss/scripts/wss-append-only.sh` that apply and bind, but nothing detects a breach yet. A check could exist.

A row moves to top-level `WSS.RULES-APPEND.md` when its check is built.

**Source:** wss/logs/WSS.DECISIONS.md's `2026-08-17 (eighth)` entry.
